# 0.8.0 需求文档 — 做减法：攘外必先安内

> 创建日期：2026-08-20
> 决策人：项目所有者
> 本期主题：**做减法**。清理死代码/死命令/过期注释/文档偏差，拆分职责过载文件，消灭确定性 bug 与架构级 bug，做必要的前瞻架构优化。**不做新功能**。

---

## 0. 范围与铁律（实现前必读）

### 0.1 涉及仓库（4 个）

| 仓库 | 本地路径 | 角色 |
| --- | --- | --- |
| bgd_sce_tools | `d:/sce_online/Res/maps/bgd_sce_tools` | 宿主工具（主视角） |
| bgd_sce_appsdk | `d:/sce_online/Res/maps/bgd_sce_plugins` | 应用公共 SDK + registry |
| sce_app_visual-injector | `d:/sce_online/Res/maps/sce_app_visual-injector` | 触编注入应用 |
| sce_app_editor-patch | `d:/sce_online/Res/maps/sce_app_editor-patch` | 编辑器补丁应用 |

### 0.2 明确排除（本期一律不碰）

- **`d:/sce_online/Res/maps/bgd_sce_framework` 整个仓库**：框架已有新版本在作者自用自调中，评估发现的框架侧问题（P-004 API key、P-005/P-006 Lua 崩溃、P-007 骨架写法、P-018~P-030 框架文档与基础库、P-021 emmyrc、P-092 release.yml 条目等）随新版本自然消亡或由作者另行处理。**本期任何任务不得修改该仓库任何文件。**
- 因排除 framework 而**暂缓**的跨仓库事项：
  - P-008（框架「版本号=tag、内容=main 快照」双轨制）：修复主体在 tools 的 `project.rs`，但方案依赖框架新版本的发布策略，**待框架新版本定版后另行决策**，本期不动。
  - RQ-22（require 改写/软覆盖防护检测）：依赖框架侧约定，暂缓。
  - P-011（产物 sha256 校验链）/ P-036（MCP 桥鉴权 token）/ P-079（请求体上限）：需要改发版 CI 与桥协议，属于"加法"，留待后续版本。
  - P-052（init 下载进度）、P-081（代理提示）、P-082（app-release.json schema）、P-083（bus factor 手册）：留待后续版本。
  - P-038（四仓库版本号注入机制各自为政）：现状无故障、靠注释维系可运行，收敛统一属流程建设（"加法"），留待后续版本。
  - P-065（appsdk 窗口居中 1920x1080 硬编码兜底）、P-067（appsdk 配置读改写非原子）：低概率低收益，留待后续版本。
  - P-066（appsdk 日志 unix 秒时间戳）不暂缓——随 WP-03 的 appsdk 发版顺手处理（一行级改动）；P-080（pie_capture 注入模板位置误导）不暂缓——随 WP-06 以注释方式核销。

### 0.3 问题编号溯源

本文所有 P-xxx 编号来自问题汇总报告：`d:/sce_online/Res/maps/bgd_sce_tools/doc/research/05-问题清单与改进建议.md`（每条含位置/现象/影响/建议修复方向）。更详细的上下文可回溯同目录 `01-宿主工具评估.md`、`03-应用生态评估.md`、`04-跨仓库一致性与流程评估.md`。需求条目形态见 `d:/sce_online/Res/maps/bgd_sce_tools/doc/requirements/audit-follow-ups.md`（RQ 编号）。

**注意**：报告中的行号是 2026-08-20 快照，实现前必须重新打开文件核对实际位置（代码可能已漂移），以实际代码为准。

**源码复核状态（2026-08-20）**：本文全部条目的文件/行号/现象已逐条对照当前源码复核完毕，结论为「全部属实」；复核中发现的行号漂移已在各条目内直接修正，复核新发现的问题以「（复核新增）」标记插入对应工作包。

### 0.4 全局约束

1. **文档同提交**：凡改动用户可见行为/命令/机制，对应 AGENTS.md、README.md 必须在同一提交内更新（各仓库 AGENTS.md 均有此硬性约定）。
2. **提交规范**：Conventional Commits（`feat: / fix: / docs: / refactor: / chore:` 前缀），一个工作包一个或多个原子提交，不打 tag、不触发发版。
3. **路径写法**：文档与说明中的路径统一使用正斜杠 `/`（Windows 工具链均兼容），不使用 `\`。
4. **验证命令**（改完必跑，全绿才允许提交）：
   - bgd_sce_tools：`cargo check`（`src-tauri/` 下）→ `cargo build` → 前端 `node node_modules/typescript/bin/tsc`（pnpm 在本机沙箱不稳定，用 npm 生态）→ **CLI 在标准验证项目验证**（构建/监听/应用市场等受影响命令，见 0.5 节）。编译/写文件可直接在仓库内进行（早期「沙箱写白名单外、需在 bgd_glzy 临时副本编译」的限制已过期，本期已实测可直接写仓库文件；若个别环境遇到写权限问题再回退临时副本方案）。
   - bgd_sce_appsdk：`cargo build` + `cargo test`。
   - sce_app_visual-injector：`cargo check` + `cargo build`。
   - sce_app_editor-patch：`cargo check` + `cargo test`（测试不碰真实编辑器，可全量跑）。**前置条件**：`src/core/bridge_deploy.rs:18-21` 用 `include_bytes!` 硬嵌入 `csharp/bgd_mcp_bridge/bin/x64/Release/bgd_mcp_bridge.dll`，该 dll 不存在时 cargo 无法编译——本机已有构建产物即可直接跑；干净克隆/清理后需先 `dotnet build csharp/bgd_mcp_bridge -c Release -p:Platform=x64 -p:SceHostDir=ci/refs` 一次（本期不改 C# 桥，无需重复构建）。
5. **网络**：本机拉依赖/调 GitHub API 需代理，命令前设 `$env:HTTP_PROXY` / `$env:HTTPS_PROXY` = `http://127.0.0.1:7897`。
6. **终端中文 GBK 乱码**属显示问题，不影响文件内容，不要因此"修复"文件编码。
7. 做减法原则：**能删就不留**；删代码时同步删其专属注释/文档/类型声明，不留 `// removed` 类墓碑注释。
8. **先验证、后动手（适用于全部工作包）**：实现每个条目前，必须先在当前代码上确证问题真实存在（复现或代码级确证），并评估文中建议方案是否可靠、可行：
   - 问题不存在（报告误判或代码已漂移修复）→ 直接核销该条目，在提交信息中注明「经核实问题不存在 + 依据」，不做任何改动；
   - 问题存在但文中方案不靠谱/不可行/成本与收益不匹配 → **实现者可自行改用更可靠的解决方案**，并在提交信息中说明偏离原因与新方案依据；
   - 文中方案只是「建议方案」，不是必须照做的契约；验收标准（问题被消除）才是契约。

### 0.5 标准验证项目（本期唯一指定）

**凡本文出现「真实项目验证」/「标准验证项目」之处，一律使用：`D:/sce_online/Res/maps/bgd_sce_veri`**（专用空白项目，有 git 存档）。原示例项目 `D:/sce_online/Res/maps/bgd_glzy` 停留在旧框架，不再作为验证项目；`C:/Users/woaye/Documents/SCE Projects/test_res002` 是作者的活跃游戏项目，不作为验证项目。

使用约定：
1. **git 存档即安全网**：项目已有 git 存档，验证产生的任何改动只要不 `git commit` 就完全可恢复（`git status` / `git diff` 可随时查看被验证命令改动了什么），无需额外备份、无操作禁区。
2. 「产物与改动前一致」类验收（如 WP-08 拆分等价性）：直接用 `git -C D:/sce_online/Res/maps/bgd_sce_veri status --short` 与 `git diff` 判断——执行构建/clean/再构建后工作区应无差异（或差异可解释）。
3. 该项目当前为未初始化的空白 SCE 项目（无 `.bgd/`）：**首次使用前执行 `bgd_sce_tools init --project D:/sce_online/Res/maps/bgd_sce_veri`**（需代理与 github_token，见 0.4 节），这一步同时即是对 init 路径的验证。

---

## 1. 工作包总览

| 编号 | 工作包 | 仓库 | 对应决策 |
| --- | --- | --- | --- |
| WP-01 | 确定性 bug 歼灭（tools） | bgd_sce_tools | 决策 5/7 |
| WP-02 | 确定性 bug 歼灭（editor-patch） | sce_app_editor-patch | 决策 7 |
| WP-03 | 单实例信号前缀契约统一 | appsdk + editor-patch + tools | 决策 7/8 |
| WP-04 | 死代码与重复实现清理（tools） | bgd_sce_tools | 决策 1/8 |
| WP-05 | 死代码与重复实现清理（appsdk / editor-patch / visual-injector） | 三仓库 | 决策 1 |
| WP-06 | 过期注释清理 | 跨仓库 | 决策 2 |
| WP-07 | 文档偏差核销（AGENTS / README / 规范文档） | 跨仓库（不含 framework） | 决策 3/4 |
| WP-08 | 职责过载拆分 | tools / editor-patch / visual-injector | 决策 6 |
| WP-09 | 安全小项（低成本收口） | tools | 决策 8 |
| WP-10 | 质量门禁（最小 CI） | appsdk / editor-patch | 决策 8 |

**执行顺序与依赖**：
- WP-01 / WP-02 / WP-06 / WP-07 互相独立，可并行（不同仓库天然并行）。
- WP-03 内部有顺序：先改 appsdk 并发版（crates.io 公开包，tag 触发 publish.yml），再升级 editor-patch 的依赖版本并改 cli.rs，最后用 tools 联动验证。
- WP-04 / WP-05 必须在同文件的 WP-01 / WP-02 修复**之后**进行（避免同一文件双线改动冲突）。
- WP-08（拆分）必须在同仓库所有代码改动（WP-01/02/04/05）**完成之后**进行——拆分是纯粹的移动代码，最后做可保证"拆分前后行为一致"的验证最有意义。
- WP-09 / WP-10 独立，可任意时间插入。

---

## WP-01 确定性 bug 歼灭（bgd_sce_tools）

> 参考：`doc/research/01-宿主工具评估.md` §4；`05` 报告 P-001/P-002/P-009/P-013/P-046。

### W1-1 修复自我更新下载 URL 必然 404（P-001，高）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/updater.rs:71`
- **问题**：下载 URL 用 `env!("CARGO_PKG_NAME")` 拼出 `repos/bgd_sce_tools/...`，缺 owner，必然 404；同文件已有正确的 `SELF_REPO` 常量。
- **改动**：改用 `SELF_REPO` 常量拼 URL。
- **验收**：URL 形态为 `repos/woaye168/bgd_sce_tools/...`；`cargo check` 通过。

### W1-2 清理 CLI 死命令消除 panic（P-002，高；决策 5）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/cli.rs:211-226, 391`
- **问题**：CMDS 白名单含 `editor`/`logs`/`mcp` 三个从 editor-patch 复制来的死入口，match 无分支落 `unreachable!()` 进程 panic。
- **改动**：从 CMDS 移除三项，并删除 match 末尾的 `_ => unreachable!()` 臂（未知命令已由 `run()` 入口 `cli.rs:236-239` 拦截返回退出码 2，不会到达 match）。USAGE 文本（`cli.rs:12-43`）经核实本就不含这三项，无需改动。
- **验收**：`bgd_sce_tools editor` 不 panic，提示未知命令并以非 0 码干净退出；USAGE 与 CMDS 一致；`cargo check` 通过。

### W1-3 CLI watch 补齐状态文件契约（P-009，中）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/cli.rs:299-317`；状态文件读写逻辑在 `builder.rs:51-60` 附近（`.watch_state.json`）。
- **问题**：CLI watch 不写 `.watch_state.json`，而 check-watch 完全依赖该文件 + PID 校验，CLI 监听期间必误报「未监听」。
- **改动**：CLI watch 分支启动后写状态文件（字段与 GUI 路径一致：PID + started_at），正常退出（含 Ctrl+C）时删除。
- **验收**：标准验证项目（见 0.5 节）上 CLI watch 运行期间另开终端 `check-watch` 返回监听中；停止后返回未监听。

### W1-4 配置 JSON 解析失败显式报错（P-013，中）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/config.rs:61-63, 70-74`
- **问题**：bgd_default.json / bgd.json 解析失败被 `if let Ok` 静默吞掉按空对象继续。
- **改动**：解析失败返回带文件路径与原因的错误，GUI/CLI 透传展示。
- **验收**：在标准验证项目（见 0.5 节）人为制造 `.bgd/bgd.json` 语法错误后执行 `bgd_sce_tools build`，报错明确指出文件与原因（验证后 `git checkout` 还原 bgd.json 即可）。

### W1-5 pid_alive 校验进程名（P-046，低但影响 watch 正确性）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/builder.rs:68-88`（pid_alive；is_watching 在 92-107）
- **问题**：只校 PID 不校进程名，PID 复用可误判「监听中」。
- **改动**：校验映像名为当前 exe 名（或比对状态文件 started_at 与进程启动时间）。
- **验收**：构造 PID 复用场景（手写状态文件填入已被其他进程占用的 PID），check-watch 不误报。

---

## WP-02 确定性 bug 歼灭（sce_app_editor-patch）

> 参考：`doc/research/03-应用生态评估.md` §6 M1/M2/M3/M5、L13/L16；`05` 报告 P-031/P-032/P-033/P-035/P-075/P-078。
> 本仓库所有改动跑 `cargo test` 全绿（集成测试守护整库备份/还原字节一致，是本次改动的安全网）。

### W2-1 write_atomic 真原子替换（P-031，中）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/core/crypto.rs:54-61`
- **问题**：现实现为「写临时 → 删原 → rename」，删除与改名间存在原文件缺失窗口，与安全红线「写盘一律原子替换」表述有差距。
- **改动**：Windows 上用 `MoveFileExW(MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)`（windows crate）实现真原子替换；同步修正函数注释。
- **实现注意（复核新增）**：本仓库 windows-sys 依赖（`sce_app_editor-patch/Cargo.toml:29`）当前 features 不含 `Win32_Storage_FileSystem`，MoveFileExW 需要补上该 feature。
- **验收**：`cargo test` 通过；注释与实现一致。

### W2-2 修复 strip_json_comments 的 UTF-8 缺陷（P-032，中）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/core/locate.rs:178-224`
- **问题**：逐字节 `as char` push 重编码，非 ASCII（如中文用户名路径）下 tsconfig typeRoots 解析结果损坏 → 编辑器定位失败且报错指向错误方向。
- **改动**：按 `char_indices` 遍历处理注释状态机，或先在字节层剥离注释再整体 `String::from_utf8`。
- **验收**：构造含中文路径的 tsconfig 样例做单元测试通过；现有测试全绿。

### W2-3 panic hook 落盘路径去硬编码（P-033，中）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/main.rs:31-36`
- **问题**：硬编码 `C:/Users/woaye/AppData/Local/Temp/ep_panic.txt`，其他机器目录不存在 → panic 无留痕，hook 形同虚设。
- **改动**：改 `std::env::temp_dir()` 拼接文件名（或 exe 旁目录，二选一，优先 temp_dir）。
- **验收**：代码评审确认无个人路径残留；`cargo check` 通过。

### W2-4 pie_capture 跨版本静默失效改显式提示（P-035，中）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/core/kernel.rs:416-453`（插槽应用逻辑）；slot 实况：仅 `slots/xdeditor/169/ui/gameplay_in_editor_view.lua` 存在拍照 slot。
- **问题**：pie_capture 模块默认启用，但行为主体 slot 仅 xdeditor 169 有，其他版本静默不生效，用户无感知。
- **改动（取兜底方案，不做运行时锚点注入）**：应用插槽/启用模块时检测当前 xdeditor 版本是否存在 pie_capture slot，缺失则在 UI 明确提示「当前编辑器版本不支持拍照修复」（状态文案），不允许静默。
- **验收**：代码路径上非 169 版本启用 pie_capture 出现明确提示；169 版本行为不变；`cargo test` 通过。

### W2-5 engine_root 回退加校验（P-075，低）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/core/locate.rs:77-83`
- **问题**：engine_root 路径假设不符时静默回退到错误根目录，后续 logs 定位全错且不报错。
- **改动**：回退时校验候选根下 `<候选>/version-<api>` 存在，否则返回明确错误。
- **验收**：`cargo test` 通过；错误信息含候选路径。

### W2-6 backup manifest 改 serde_json 序列化（P-078，低）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/core/backup.rs:67-71`
- **问题**：手写 JSON 字符串拼接，路径含引号即非法 JSON，且写失败静默。
- **改动**：改 serde_json 序列化；写失败返回错误。
- **验收**：`cargo test` 通过（备份/还原字节一致集成测试必须仍绿）。

---

## WP-03 单实例信号前缀契约统一（P-037，中；决策 8 前瞻）

> 参考：`doc/research/04-跨仓库一致性与流程评估.md` §3 F-M1；`03` 报告 L12。
> **问题链**：宿主安装时把应用 exe 重命名为 `apps/<id>/<id>.exe`（`bgd_sce_tools/src-tauri/src/apps.rs:280`）→ appsdk 按 exe 文件名推导单实例/信号前缀为 `editor-patch`（`bgd_sce_plugins/src/app.rs:35-40` 的 `default_si_prefix`）→ editor-patch CLI 硬编码旧名 `signal_refresh("sce_app_editor-patch")`（`sce_app_editor-patch/src/cli.rs:77`）→ 宿主切项目 notify 后运行中的 GUI 收不到 refresh 事件。

### W3-1 appsdk 开放前缀推导
- **文件**：`d:/sce_online/Res/maps/bgd_sce_plugins/src/app.rs:35-40`
- **改动**：`default_si_prefix`（或等价推导函数）提为 `pub`，使应用方可复用同一推导，消灭第二处硬编码。
- **同批 appsdk 改动（避免二次发版，复核编排修订）**：WP-05 的 P-072 需要 appsdk 侧先把日期算法开放复用——`d:/sce_online/Res/maps/bgd_sce_plugins/src/log.rs:44-61` 的 `today()`（含 civil_from_days 算法）提为 `pub`；并顺手核销 P-066——`log.rs:32-36` 的 unix 秒时间戳改为本地时间格式化字符串。以上 appsdk 侧改动与 W3-1 一并提交、一并发布（W3-4 的第 1 步），editor-patch 侧的消费改动仍归 WP-05。

### W3-2 editor-patch 改用同一推导
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/cli.rs:77`
- **改动**：删掉硬编码 `"sce_app_editor-patch"`，改调 appsdk 公开的推导函数。

### W3-3 契约文档化
- **文件**：`d:/sce_online/Res/maps/bgd_sce_plugins/AGENTS.md`（「使用方约定」段）
- **改动**：补一条命名契约——「宿主按 `<id>.exe` 落盘，单实例/信号前缀一律由 appsdk 按 exe 名推导，应用方禁止硬编码」。

### W3-4 发版与联动验证（顺序敏感）
1. appsdk 提交并发 patch 版（tag 触发 publish.yml 上 crates.io）；
2. editor-patch 升级 `Cargo.toml` 中 `bgd_appsdk` 版本依赖并提交；
3. 用 bgd_sce_tools 在标准验证项目（见 0.5 节）联动验证：安装 editor-patch → 启动 → 执行 `<exe> notify project_path=D:/sce_online/Res/maps/bgd_sce_veri` → GUI 项目栏实时刷新。
- **验收**：上述链路实测通过；三仓库 `cargo check/test` 全绿。

---

## WP-04 死代码与重复实现清理（bgd_sce_tools；决策 1/8）

> 参考：`01` 报告 §4 L1-L12、M4、M7、§2.2；`05` 报告 P-041/P-015/P-017/P-044/P-012/P-045/P-047/P-048/P-049/P-050/P-051/P-053。
> 前置：WP-01 已完成。

### W4-1 删除同步版死代码（P-041，约 100 行）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/apps.rs`、`updater.rs`
- **改动**：删除确认无调用方的同步版函数：`apps.rs:101` `fetch_registry`、`:129` `enrich_registry`、`:136` `enrich_app`（同步版）、`:171` `fetch_release_meta`（同步版）、`:236` `install_app`（同步版）、`:297` `install_app_with`、`:94` `app_dir_exists`、`updater.rs:108` `start_self_update`（同步版）。**复核结论**：以上函数已全仓 Grep 确认无外部调用方（同步版 enrich_app/fetch_release_meta/install_app_with 仅被同批死代码互相引用，属传递性死代码，一并删除）。
- **验收**：`cargo check` 无新增警告；全仓 Grep 无引用。

### W4-2 进程枚举去重（P-015）
- **文件**：`apps.rs:388-415` 与 `431-463`
- **改动**：PowerShell 进程枚举复制粘贴两份，抽 `query_processes()` 公共函数两处复用。

### W4-3 前后端配置契约清理（P-017 + P-053）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src/lib/types.ts:6-8`；对照 `src-tauri/src/config.rs:12-46`；`src/pages/SettingsPage.tsx`
- **改动**：① 删除 types.ts 中 Rust 端不存在的 3 个幻影字段（`asset_target` 等，保存时被静默丢弃）；② `enable_build_log` / `libs_excludes` / `game_excludes` 三个真实字段在设置页 UI 注明「请使用 CLI config set 修改」（本期不加表单，只消歧）。
- **验收**：types.ts 与 config.rs 字段一一对应；前端 tsc 通过。

### W4-4 小项清理批（同仓库顺手做）
| 编号 | 文件:行 | 改动 |
| --- | --- | --- |
| P-044 | `builder.rs:172` | 移除 `rewrite_res_paths` 未使用的 `_cfg` 参数 |
| P-012 | `builder.rs:148, 156, 181` | 循环内 `Regex::new` 改 `std::sync::LazyLock` 静态化（每文件 12 次正则编译 → 全局 1 次） |
| P-045 | `builder.rs:988-1024` | 监听去重循环加兜底：每次收事件顺带扫描处理到期项，防持续流入时永不处理 |
| P-047 | `lib.rs:504` | format 内联命名参数简化 |
| P-048 | `lib.rs:43-52` | `write_log_file` 每行 open+close 改缓存句柄或批量 flush |
| P-049 | `lib.rs:184-201` | 注册表 PATH 检查/写入成功后落标记文件，后续启动跳过 spawn PowerShell |
| P-050 | `src/components/LogPanel.tsx:20, 39` | 日志行 key 从数组下标改递增 id；smooth 滚动加节流 |
| P-051 | `src/pages/AppPage.tsx:28-43` | `toggleAutoStart` 乐观更新失败时在 catch 中回滚 state |

- **验收**：`cargo check` + 前端 tsc 通过；CLI 在标准验证项目（见 0.5 节）跑 `build` / `watch` / `check-watch` / `app` 相关命令回归正常。

---

## WP-05 死代码与重复实现清理（appsdk / editor-patch / visual-injector；决策 1）

> 参考：`03` 报告 §6 L1/L5/L7/L8/L9/L14、§7 D5/D6/D8；`05` 报告 P-064/P-068/P-070/P-071/P-072/P-076/P-089/P-090。
> 前置：同仓库的 WP-02 / WP-03 已完成。

### W5-1 appsdk 清理
- **文件**：`d:/sce_online/Res/maps/bgd_sce_plugins/src/single_instance.rs:107-109`、`src/ui.rs`（误导性旧方案注释）、`src/bin/main.rs:219`
- **改动**：① 删除死代码 `signal_show_self` 及 ui.rs 中指向它的旧方案注释（P-064）；② 脚手架模板文案矛盾修正——模板 AGENTS 写「git 依赖」、Cargo.toml 写 crates.io，统一为 crates.io（P-068）。

### W5-2 editor-patch 重复实现去重
| 编号 | 文件 | 改动 |
| --- | --- | --- |
| P-070 | `src/core/slot_inject.rs:13-31` | MAGIC/KEY/XOR 解密与 crypto.rs 重复实现，改 `pub use`/调用 crypto 的统一实现，密钥单一真相 |
| P-071 | `src/core/editor.rs:181-205` 与 `src/core/capture.rs:114-142` | `find_editor_pid` 整段重复，提取公共函数（可放 editor.rs，capture 复用） |
| P-072 | `src/core/editor.rs:311-323` vs appsdk `src/log.rs:44-61` | civil_from_days/today 日期算法两处重复，editor.rs 侧改调 appsdk 开放版本（appsdk 侧的开放已在 WP-03 同批发版完成，此处只需升级依赖版本并删本地实现） |

### W5-3 删除两应用的 clap 冗余依赖（P-089/P-090）
- **文件**：`d:/sce_online/Res/maps/sce_app_visual-injector/Cargo.toml`、`d:/sce_online/Res/maps/sce_app_editor-patch/Cargo.toml`（及 Cargo.lock）
- **问题**：两仓库 AGENTS.md 均声称技术栈含 clap，实际全仓无任何使用（CLI 均手写分发），纯冗余依赖。
- **改动**：从 Cargo.toml 删除 clap，`cargo check` 确认无残留引用。

### W5-4 visual-injector JSON id 稳定化（P-076，先实测再定）
- **文件**：`d:/sce_online/Res/maps/sce_app_visual-injector/src/core.rs:802, 840, 911, 1094`
- **问题**：触编 JSON 的 id 每次注册随机重生成，若触编按 id 引用则既有引用失效（风险待实测）。
- **改动**：先实测确认触编引用方式——若按 name 引用，则在 `doc/visual_injection_spec.md` 注明「id 不稳定但无影响」即可，不改代码；若按 id 引用，id 改「模块+函数名」稳定哈希。
- **实现注意（复核新增）**：rand 的 4 处调用（`core.rs:801/864/910/1093`）全部用于 id 生成；若落地稳定 id，rand 依赖随之成为冗余，从 `Cargo.toml` 一并删除。
- **验收**：实测结论写入规范文档；如改代码则重复注册同一模块 id 不变。

---

## WP-06 过期注释清理（决策 2）

> 参考：`05` 报告 P-042/P-043/P-069/P-073/P-074/P-091；`03` 报告 D2/D3/D10；`01` 报告 L2/L3/D7。
> 原则：注释描述与现行行为不一致的一律改或删；不留墓碑注释。

| 编号 | 文件:行 | 问题与处理 |
| --- | --- | --- |
| P-042 | `bgd_sce_tools/src-tauri/src/builder.rs:169-171` | 同段文档注释连续重复两遍 → 去重 |
| P-043 | `bgd_sce_tools/src-tauri/src/builder.rs:467-468` | 残留「res 声明文件解析」注释，功能已不存在 → 删 |
| P-069 | `sce_app_editor-patch/src/core/crypto.rs:4-5` | 头注释「加密→加密写回」与现行「写回一律明文」策略矛盾 → 更新为现行策略描述 |
| P-073 | `sce_app_editor-patch/src/core/capture.rs:12` | 头注释「最小化时无帧可截」已被 WindowRestoreGuard 离屏恢复实现推翻 → 更新 |
| P-074 | `sce_app_editor-patch/src/core/modules.rs:185-189` | 「同步注入的项目根」注释段错位悬空 → 归位或删 |
| P-091 | `sce_app_editor-patch/src/mcp.rs:6` | 头注释工具名 `get_logs`，实际为 `get_game_logs` → 改 |
| （随 W5-1） | `bgd_sce_plugins/src/ui.rs` | 指向 signal_show_self 的误导性注释 → 随死代码一起删 |
| P-080 | `sce_app_editor-patch/patches/xdeditor/pie_capture/snapshot_handler.lua` | 该文件是 make_pie_slot 的注入模板（`src/core/modules.rs:85-88` 只挂 main.lua），放在模块目录易被误读为随模块部署 → 文件头加注释明示「仅供 examples/make_pie_slot 使用，不随模块部署」 |
| （复核新增） | `bgd_sce_tools/src-tauri/src/builder.rs:3` | 头注释「逻辑与 Python 参考实现（.bgd/tools_py/bgd_build.py）一致」——框架模板已不存在 tools_py，参考实现已消亡 → 删除该句 |

- **验收**：以上位置逐一核销；同时各仓库快速扫一遍改动文件的头注释，发现同类过期一并处理（仅限本期已触碰的文件，不扩大战场）。

---

## WP-07 文档偏差核销（决策 3/4；不含 framework）

> 参考：`05` 报告 §1.4 与 §2.5；`04` 报告 §1（23 条偏差汇总与逐条处置建议）；`01` 报告 §5；`03` 报告 §7。
> 下表为本期范围内全部偏差，逐条核销；framework 侧偏差（P-092、P-018~P-020、P-026、P-029、P-060、P-063）**不在本期**。

### W7-1 bgd_sce_tools — AGENTS.md 与 README.md
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/AGENTS.md`、`d:/sce_online/Res/maps/bgd_sce_tools/README.md`

| 编号 | 位置 | 偏差 → 修正 |
| --- | --- | --- |
| P-084 | AGENTS.md「发布」节 | 仍写「版本号三处手工同步（tauri.conf.json/Cargo.toml/package.json）」→ 实际已升级为 git tag 单一来源 CI 注入（见 `.github/workflows/release.yml`），改写为现行机制 |
| P-085 | AGENTS.md「CLI 子命令」节 | 补已存在但未列出的 `app <应用id>` 子命令（实现见 `src-tauri/src/cli.rs`） |
| P-086 | AGENTS.md 应用市场节 | 升级按钮表述残留旧链路 → 改为「对比 registry 补全后的 app-release.json version」现行链路 |
| P-087 | AGENTS.md CLI 节 | `setting set` 不支持 `save_log`（GUI 独有），在 CLI 表中注明能力边界 |
| — | AGENTS.md「硬性约定」节 | 验证项目示例（约 L59）**已于 2026-08-20 先行修正**为标准验证项目 `D:/sce_online/Res/maps/bgd_sce_veri`，本条仅核对确认 |
| — | AGENTS.md「注意事项」节 | 「工具链敏感目录在沙箱写白名单外，编译请在 bgd_glzy 临时副本执行」（约 L122）限制已过期（本期实测可直接写仓库），删除或改写为「如遇写权限问题再回退临时副本方案」 |
| （复核新增） | README.md「Fork 后必改清单」第 1/2 步 | 整体过期：自我更新是自建逻辑（`src-tauri/src/updater.rs`），`tauri.conf.json` 无 updater 插件段、不存在签名密钥与 `TAURI_SIGNING_PRIVATE_KEY` secrets → 删除这两步或改写为「自我更新为自建逻辑，fork 只需改 `updater.rs` 的 `SELF_REPO` 常量」 |
| （复核新增） | README.md「界面导览」与「技术栈与项目结构」 | 页面表缺「应用」页（应用市场）；结构图写「五个页面」（实际六个，`src/lib/types.ts:39` PageKey 含 apps）；src-tauri 文件清单缺 `cli.rs` / `apps.rs` → 补齐 |
| （复核新增） | README.md「CLI 子命令」节 | 清单缺 `watch` / `config` / `app` 三个子命令 → 补齐（与 cli.rs USAGE 对齐） |
| — | README.md 其余 | 「发布新版本」节已是 git tag 单一来源描述（与代码一致，无需改）；token 存储说明随 W9-3 更新（见 W9-3 改动第 5 条） |

### W7-2 sce_app_visual-injector — 注入规范文档（本期文档类最高优先级）
- **文件**：`d:/sce_online/Res/maps/sce_app_visual-injector/doc/visual_injection_spec.md` §2
- **偏差（P-088）**：SceInit 分目录描述过期，实际统一写 `.bgd/src/<side>/SceInit.lua`（实现见 `src/core.rs` 的 append/remove_sce_init）。**该文档是注入逻辑的准绳，偏差会直接误导后续改动。**
- **同时**：AGENTS.md 技术栈删 clap（随 W5-3）；README 核对无同类过期。

### W7-3 sce_app_editor-patch — AGENTS.md
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/AGENTS.md`
- **偏差（P-090）**：技术栈删 clap（随 W5-3）；README 核对无同类过期。

### W7-4 bgd_sce_appsdk — AGENTS.md
- **文件**：`d:/sce_online/Res/maps/bgd_sce_plugins/AGENTS.md`
- **改动**：补 WP-03 的单实例命名契约条款；核对「现有使用方」「CLI 脚手架」等描述与实际一致。

- **整包验收**：上表逐条核销；被修改文档内不再含有与现行代码矛盾的描述（以实际代码为准逐一比对）。

---

## WP-08 职责过载拆分（决策 6）

> 参考：`05` 报告 P-014/P-034/P-077；`01` 报告 M6；`03` 报告 M4/L15。
> **铁律：拆分为纯代码移动，不改行为；必须在同仓库所有功能改动完成后进行；拆分前后用相同验证命令证明等价。**

### W8-1 bgd_sce_tools builder.rs（1030 行 → 多模块）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/builder.rs`
- **现状**：单文件承担构建主流程 / require 改写 / res 同步 / API 聚合 / init 渲染 / 入口合并 / emmyrc / gitignore / AGENTS 同步 / 监听去重 / 状态文件等 10+ 职责。
- **方案**：按职责拆子模块（建议 `builder/` 目录：`mod.rs` 主流程编排、`watch.rs` 监听与去重、`res.rs` 资源同步、`merge.rs` 入口合并、`rewrite.rs` require 改写与正则），单文件 < 500 行。具体边界以实际代码内聚性为准，不强行均分。
- **验收**：`cargo check` 通过；标准验证项目全量构建 + clean + 再构建后，`git -C D:/sce_online/Res/maps/bgd_sce_veri status --short` 无差异（见 0.5 节第 2 条）；watch 增量行为一致。

### W8-2 sce_app_editor-patch main.rs（804 行）与 kernel.rs（861 行）
- **文件**：`d:/sce_online/Res/maps/sce_app_editor-patch/src/main.rs`、`src/core/kernel.rs`
- **方案**（本仓库自定「500 行必拆」约定）：main.rs 业务 UI 按标签页拆 `ui_kernel.rs` / `ui_patches.rs` / `ui_settings.rs`（以实际 UI 分区为准）；kernel.rs 内联的 `mod slots` 独立为 `src/core/slots.rs`。
- **验收**：`cargo test` 全绿；单文件 ≤ 500 行（入口聚合类文件可略宽限，需在模块头注释说明）。

### W8-3 sce_app_visual-injector core.rs（1315 行）
- **文件**：`d:/sce_online/Res/maps/sce_app_visual-injector/src/core.rs`
- **方案**：按既有职责拆 `parser.rs`（扫描与 Lua 解析）/ `json_gen.rs`（FunctionDefine/Class JSON）/ `sce_init.rs`（SceInit 追加移除），core.rs 仅留编排。
- **验收**：`cargo check` 通过；对标准验证项目分别用拆分前后版本生成注入产物，用 `git status` / `git diff`（见 0.5 节第 2 条）确认产物无差异（W5-4 的 id 稳定化若落地，则以其结论为准比对）。

---

## WP-09 安全小项（决策 8；低成本、不动协议与 CI 的三件）

> 参考：`05` 报告 P-003/P-010/P-016；`01` 报告 H3/M2/M8。
> （P-011 产物哈希链与 P-036 桥鉴权属"加法"，已按 0.2 节明确留待后续版本。）

### W9-1 registry app.id 字符校验（P-010）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/apps.rs:84-91, 236-294`
- **改动**：安装/卸载/启动前校验 `id` 匹配 `^[a-zA-Z0-9_-]+$`，不匹配即拒绝并报错，杜绝路径穿越写出 `apps/` 外。
- **验收**：构造 `id: "../x"` 的 registry 样例，安装被拒绝且 apps/ 外无写入。

### W9-2 WebView 最小 CSP（P-016）
- **文件**：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/tauri.conf.json:23`
- **改动**：`csp: null` 改最小可用 CSP（如 `default-src 'self'` 起步，按实际资源加载需求微调）。
- **验收**：GUI 全部页面功能正常，devtools 无 CSP 违例报错。

### W9-3 github_token 迁入 Windows 凭据管理器（P-003，高；架构级安全问题，本轮代码落地）
- **问题**：token 明文存 `%APPDATA%/com.bgd.sce-tools/settings.json`，且是四个私有仓库全链路唯一凭证（框架下载/应用市场/自我更新均依赖它）。
- **源码现状（已核实，实现前按 0.4 节第 8 条复核）**：
  - 字段定义：`d:/sce_online/Res/maps/bgd_sce_tools/src-tauri/src/project.rs:28`（`AppSettings.github_token`，随结构体整体 serde 序列化）
  - 读口子：`project.rs:47-53` `load_settings`；写口子：`project.rs:55-62` `save_settings`
  - 消费方（全部经上述两口子和 `AppSettings` 结构体，**方案设计保证它们零改动**）：`src-tauri/src/cli.rs:259, 265, 286`（读取）、`cli.rs:356`（setting set 写入）、`src-tauri/src/lib.rs:212`（GUI 命令读取）；前端 `src/lib/types.ts:83`、`src/pages/SettingsPage.tsx:10, 181-182`（表单编辑，字段语义不变）
- **方案选型（Windows 凭据管理器）**：用 `keyring` crate（3.x，Windows 后端为 wincred，即 Windows 凭据管理器，Git Credential Manager 同款机制）。**否决 DPAPI 存文件方案**：两者都不防「同用户针对性进程」（凭据管理器可 CredRead、DPAPI 可 CryptUnprotectData），但凭据管理器让 token 从文件中彻底消失——偶然性扫盘/备份外泄/误分享/误提交场景完全消除，且是 Windows 平台凭证存储标准做法；DPAPI 则把密文留在原文件里，无额外收益。
- **改动**：
  1. `src-tauri/Cargo.toml` 加依赖 `keyring = "3"`（默认特性即可，Windows 上走 wincred）；
  2. 新建 `src-tauri/src/secret.rs`：`KEYRING_SERVICE = "bgd_sce_tools"` / `KEYRING_USER = "github_token"`；`load_github_token() -> String`（未找到返回空串）、`save_github_token(&str) -> Result<()>`（空串 → 删除条目并忽略 NotFound，非空 → set_password）；
  3. `project.rs`：
     - `AppSettings.github_token` 加 `#[serde(skip)]`（永远不落盘）；
     - `load_settings`：先按 `serde_json::Value` 读原文件做**迁移检测**——若 JSON 中仍含非空 `github_token` 明文，则写入凭据管理器、从 Value 中移除该键并把 scrub 后的文件写回；随后反序列化 AppSettings，再用 `secret::load_github_token()` 填充字段返回；
     - `save_settings`：序列化落盘（github_token 已被 skip），另调 `secret::save_github_token(&settings.github_token)`；
  4. 消费方与前端零改动（字段语义不变：读到的是可用 token，写入即生效）；
  5. 文档同提交：`AGENTS.md`「私有仓库认证」段更新存储机制描述 + 补残余风险明示（「不防同用户针对性进程读取凭据管理器；PAT 必须保持 fine-grained Contents 只读，泄露立即吊销轮换」）；`README.md:58`「Token 仅存于本机应用设置（settings.json）…」一句改为凭据管理器表述（README:56 与 :141 的 CLI 用法示例不变）。
- **验收**：
  1. 迁移：构造含明文 token 的 settings.json → 启动后 token 自动进入凭据管理器（控制面板「凭据管理器 → Windows 凭据」可见 `bgd_sce_tools/github_token`），settings.json 中该字段消失；
  2. 功能：`setting set/get github_token`、GUI 设置页编辑保存、检查框架更新/应用市场拉取全部正常；重启系统后仍可读；
  3. `cargo check` 通过；新增依赖经 `cargo build` 验证（需代理，见 0.4 节）。
- **配套动作（非代码）**：确认当前 PAT 为 fine-grained 且仅 Contents 只读（真实爆炸半径控制），结果记入 AGENTS.md 上述段落。

---

## WP-10 质量门禁：最小 CI（决策 8）

> 参考：`05` 报告 P-039；`04` 报告 §3 F-M4。
> 本期只做零成本两件，不做全量 CI 建设。

### W10-1 appsdk publish 门禁
- **文件**：`d:/sce_online/Res/maps/bgd_sce_plugins/.github/workflows/publish.yml`
- **改动**：发布前加 `cargo test` 步骤，失败阻塞发布（当前 publish 不跑任何测试，已核实：workflow 仅 注入版本 → cargo publish 两步）。
- **验收**：workflow 语法有效；`cargo test` 失败时 publish 步骤不执行。

### W10-2 editor-patch CI
- **文件**：新建 `d:/sce_online/Res/maps/sce_app_editor-patch/.github/workflows/ci.yml`
- **改动**：push/PR 触发。**步骤顺序已核实（复核修订）**：`actions/setup-dotnet@v4`（dotnet 9.0.x）→ `dotnet build csharp/bgd_mcp_bridge -c Release -p:Platform=x64 -p:SceHostDir=ci/refs`（**必须先行**：`src/core/bridge_deploy.rs:18-21` 的 `include_bytes!` 硬嵌入该构建产物，本仓库无 build.rs 兜底，`ci/refs/` 只是 .NET 编译用引用程序集、不含桥 dll 本身）→ `cargo test`（本仓库测试均为临时目录集成测试，不碰真实编辑器，可直接上 CI）。
- **验收**：workflow 语法有效（可用 act 或推测试分支验证，验证后删测试分支）；失败时能正确阻塞。

---

## 2. 完成定义（Definition of Done）

1. WP-01 ~ WP-10 全部工作包验收标准逐条通过。
2. 四仓库验证命令全绿（见 0.4 节第 4 条）。
3. 所有改动遵循文档同提交约束；本期范围内 05 报告对应条目（P-001/002/003/009/010/012/013/015/016/017/031/032/033/035/037/041~051/053/064/066/068/069/070/071/072/073/074/075/076/078/080/084~091）全部核销；范围内其余条目（P-008/011/036/038/040/052/065/067/079/081/082/083 与全部 framework 条目）已按 0.2 节明示暂缓，不属于本期完成定义。
4. `bgd_sce_framework` 仓库零改动（`git -C d:/sce_online/Res/maps/bgd_sce_framework status` 干净）。
5. 验证项目 `D:/sce_online/Res/maps/bgd_sce_veri` 的验证改动不入 git 提交（工作区改动可保留或还原，由验证者收尾决定）。
6. 不打任何发版 tag（本轮迭代后的代码需由人工审核后确认无误后再打 tag）。
